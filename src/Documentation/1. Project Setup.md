# Setup The React Project

Run `npm create vite@latest mktsystem-react -- --template react-ts` to create the react project

Choose no for the default question during installation

Then, we run below command:

```shell
cd mktsystem-react
npm install
npm run dev
```

Then run `npm install tailwindcss @tailwindcss/vite `to install tailwind (This part may failed because of latest vite therefore downgrade the necessary vite and plugin)

### Configure the vite plugin

Add the @tailwindcss/vite plugin to your Vite configuration.

```tsx
export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

---

### Add Tailwind to CSS

Open `src/index.css` and replace everything with:

```tsx
@import "tailwindcss";
```

---

### How to try

Open src/App.tsx and replace everything with:

```tsx
function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-green-500 text-white text-3xl">
      Tailwind is working ✅
    </div>
  );
}

export default App;
```

Then run `npm run dev`
